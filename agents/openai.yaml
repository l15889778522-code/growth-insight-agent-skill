interface:
  display_name: "Growth Insight Multi-Agent"
  short_description: "Seven-agent sequential growth analysis pipeline"
  default_prompt: "Use $growth-insight-agent-skill to analyze my data request. Spawn all seven agents sequentially, save each stage report, and pass it to the next agent."

policy:
  allow_implicit_invocation: false
