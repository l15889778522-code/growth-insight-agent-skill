interface:
  display_name: "Growth Insight Multi-Agent"
  short_description: "Seven-agent analysis with approval gates"
  default_prompt: "Use $growth-insight-agent-skill to analyze my data request. Start only the first agent, show me its report, and wait for my explicit confirmation before starting each next agent."

policy:
  allow_implicit_invocation: false
